/*
 
 1. 总结数据类型.
 
    1). 基本数据类型.
        int
        double
        float
        char
 
    2). 构造类型.  构建创造.  
        数组.
        结构体.
        枚举.
 
 
    3). 指针.
 
 
    4). void 空类型.
 
 
    5). typedef 定义类型.
 
        typedef int itheima;
 
 
 2. IF结构.
 
    1). 如果if块中只有1句代码,那么大括弧可以省略.
 
    2). 如果if块后面没有大括弧.你记住,只有第1句代码才是属于这个if块的.
 
    3). 如果if块中只有1个语法块,那么这个时候if的大括弧也能省略.
 
 
 3. 循环也是一样的.
 
 
 
 4. 忠告.
    
    1) 虽然有时候,可以省略if或者循环大括弧,但是我不建议你省略,因为这样的可读性很低.
 
    2).就算你要省略,那么最好也是Tab缩进.
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 */


#include <stdio.h>

int main(int argc, const char * argv[])
{
    
//    int num = -1;
//    if(num > 0)
//    for(int i = 0; i < 10; i++)
//    printf("%d\n",i);
    
//    if(num > 0)
//        printf("AAAAA\n");
//    else
//        printf("BBBBBBB\n");
//    printf("CCCCC\n");
//    
    
    

    
    
    return 0;
}
